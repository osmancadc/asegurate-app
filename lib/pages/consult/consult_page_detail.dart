import 'package:flutter/material.dart';

class ConsultPageDetail extends StatelessWidget {
  const ConsultPageDetail({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('ConsultPageDetail'),
      ),
      body: Center(
        child: Text('ConsultPageDetail'),
      ),
    );
  }
}
